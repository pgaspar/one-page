<h1><%= @page.title %></h1>
<h2><%= @page.subtitle %></h2>

<%= image_tag @page.cover_photo.url if @page.cover_photo.file? %>

<br />

<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>
