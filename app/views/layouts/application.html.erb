<!DOCTYPE html>
<html>
<head>
  <title>Railsrumble</title>
  <%= include_stylesheets :defaults %>
  <%= include_javascripts :defaults %>
  <%= csrf_meta_tags %>
</head>
<body>

    <% if notice %><p class="notice"><%= notice %></p><% end %>
    <% if alert %><p class="alert"><%= alert %></p><% end %>

    <%= yield %>

<!--
  <% if signed_in? %>
    <%= link_to 'Manage your Pages', :pages %> | <%= link_to 'Logout', :destroy_user_session %>
  <% else %>
    <%= link_to 'Sign In', :new_user_session %>
  <% end %>
-->

</body>
</html>
